Task PAY-1: New logo in the header
Repository: /Users/me/Code/web
Working directory: /Users/me/.nodal/worktrees/web/pay-1-logo
This is a git worktree dedicated to this task, on branch `nodal/pay-1-logo` (based on `main`). Work only here and stay on this branch.

## Plan
The plan is at /data/tasks/t1/plan.md.
<plan>
# Logo
Replace the header logo with the new one.
</plan>

## Acceptance criteria
- The new logo shows up in the header
- The tests pass

## When you finish
Commit your changes, push the branch and open a pull request with `gh pr create`. Never merge it.
End your final message with a fenced JSON block, exactly in this shape:
```json
{"status": "done", "summary": "<one paragraph: what you did and anything a reviewer should know>", "pr": null, "branch": "<branch name, or null>"}
```
Use "status": "blocked" and explain why in "summary" if you couldn't finish. Put the pull request URL in "pr" if you opened one.